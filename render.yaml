services:
  - type: web
    name: messenger-chatbot
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: VERIFY_TOKEN
        sync: false
      - key: PAGE_ACCESS_TOKEN
        sync: false
    healthCheckPath: /
